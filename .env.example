# Copy to .env for local development (optional; config.development.yaml has defaults).

# Match local Mongo after auth + replica set setup (admin/password, rs0)
MONGO_URI=mongodb://admin:password@127.0.0.1:27017/?replicaSet=rs0&authSource=admin
SWAGGER_ENABLED=true
SWAGGER_PASS=changeme
